const swiper = new Swiper(".mySwiper", {
  pagination: {
    el: ".swiper-pagination",
  },
});

const sliderDots = new Swiper(".sliderDots", {
  pagination: {
    el: ".swiper-pagination-dots",
    clickable: true,
  },
});